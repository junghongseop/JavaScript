//alert(): 경고창을 띄우는 함수
//console.log(): log를 띄우는 함수

const Name = 'Mike';    //이렇게 변수를 지정하면 안됨.
const Age = 30;         //변수가 이렇게만 있다는 보장이 안되기 때문에

alert(Name);
console.log(Age);


//let name = "Google"; 이미 name이라는 변수가 지정되어 있어 실행하면 에러가 뜸
//const name = "Google";
//const는 절대 바뀌지 않는 상수, 수정x ex)원주율, 속도, 생일

/*
자바 스크립트에서 변수를 선언할때는
변하지 않는 값은 const
변하는 값은 let으로 선언
*/

